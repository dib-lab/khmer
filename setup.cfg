[nosetests]
verbosity = 2
stop = TRUE
attr = !known_failing
#attr = !known_failing,!known_failing-lowmem
# where lowmem <= 0.5GiB memory

[build_ext]
undef = NO_UNIQUE_RC
# define = NDEBUG
# is not needed for most Linux installs
# see the OPT line in /usr/lib/python2.7/config/Makefile which gets included
# in distutils version of CFLAGS

[easy_install]

